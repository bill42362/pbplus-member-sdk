// UserUUID.js
'use strict';

const defaultState = 'mock-user-uuid';

const Reducer = (state = defaultState, action) => {
    switch(action.type) {
        default:
            return state;
    }
}

const Actions = { };

export default { Reducer, Actions };
